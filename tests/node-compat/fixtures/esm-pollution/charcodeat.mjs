// Same shape as charat.mjs, one representation down: the mask's decision about
// where a string ENDS is what keeps this text data rather than code.
import { helper } from './dep-cca.mjs';

export const doc = ";export default (globalThis.__esm_cca_pwned = 'CCA INJECTED');";
export const ok = 'CCA-OK:' + helper();
